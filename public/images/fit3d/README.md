# Fit3D SNAP media

Referenced from `FIT3D_MEDIA` in `lib/fit3d.ts` and the image registry in `lib/images.ts`.

- `scanning.jpg` — person mid-scan with app overlay (/services/body-composition hero)
- `before.png` — avatar with red metric callouts
- `after.png` — avatar with green metric callouts
- `snap-station.jpg` — SNAP station product shot (home service card)

Still placeholder: the "Powered by Fit3D" logo mark. Drop `fit3d-logo.svg` here and
point `FIT3D_MEDIA.logo` at it when the final asset is available.
